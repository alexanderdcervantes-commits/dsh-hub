{
  "name": "dsh-meme-hub-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build:data": "python3 scripts/build-data.py --enrich",
    "check:prerender": "node scripts/check-prerender.mjs"
  },
  "dependencies": {
    "@nuxtjs/i18n": "^10.4.0",
    "nuxt": "^3.14.159"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3",
    "vue-tsc": "^3.3.10"
  }
}
